@model Hayden.WebServer.View.ThreadPartialViewModel

@{
    Layout = "_Layout";
    ViewData["Title"] = Model.Thread.Title;

    Model.BoardPage = false;
}

@await Html.PartialAsync("/View/ThreadPartialView.cshtml", Model)